# 退款风控原型拓展｜项目执行阶段计划

## 1. 结论

本项目按“基线接入 → 现状盘点 → 需求收敛 → 方案设计 → 原型实现 → 浏览器验收 → 评审闭环”推进。当前工作区只有空 Git 仓库，尚未配置远端、没有提交记录，也没有可盘点的原型代码，因此必须先完成 Phase 0，后续页面范围与实现工作量才能最终锁定。

> 当前阶段：本轮仅完成执行计划，不创建分支、不拉取远端、不修改原型。

## 2. 项目目标与边界

| 项目 | 说明 |
| --- | --- |
| 总目标 | 在原有原型基础上拓展退款风控相关需求，形成可看懂、可点击、可评审、可验证的本地高保真原型 |
| 本轮交付 | 一份可直接执行和持续更新的阶段计划 |
| 目标用户 | 待基线盘点和需求访谈确认，初步可能涉及退款运营、风控审核、规则运营等角色 |
| 交付形态 | 需求收敛文档、页面/流程方案、前端原型、Mock 数据、视觉验收记录、评审反馈记录 |
| 实现原则 | 延续原项目技术栈和视觉规范；优先复用现有组件；保持完整可点击闭环 |
| 非目标 | 默认不新增后端、数据库、登录、真实风控接口、生产部署和复杂架构 |

## 3. 已确认基线

| 检查项 | 当前结果 | 对计划的影响 |
| --- | --- | --- |
| 本地目录 | 仅存在 `.git` | 暂时无法盘点原型页面、组件和运行方式 |
| 当前分支 | `master`，无提交 | 不能直接从本地创建有效功能分支 |
| Git 远端 | 未配置 | 需先接入 `erduo85-source/RCP.git` |
| 原型运行/截图 | 未执行 | 当前没有视觉基线，不能承诺具体页面改动 |
| 分支创建 | 未执行 | 按项目规则，需先确认分支名和基线分支 |

## 4. 执行总览

| 阶段 | 核心目标 | 主要交付物 | 预估投入* | 阶段门禁 |
| --- | --- | --- | --- | --- |
| Phase 0 | 接入仓库并建立安全基线 | 可用本地基线、功能分支、现状清单 | 0.5 天 | 原型代码可读、可确认基线分支 |
| Phase 1 | 盘点原型与业务上下文 | 原型现状报告、页面/流程/组件清单、基线截图 | 0.5–1 天 | 原型可运行或已明确不可运行原因 |
| Phase 2 | 收敛退款风控需求 | 需求收敛文档、范围与优先级、验收标准 | 1 天 | 用户、场景、对象、目标、非目标达成一致 |
| Phase 3 | 设计信息架构与交互闭环 | 页面方案、关键流程、状态矩阵、Mock 数据设计 | 1–2 天 | 主流程和边界状态可评审、无关键歧义 |
| Phase 4 | 拆解实现任务并完成原型 | 可点击高保真前端原型、实现记录 | 2–4 天 | 主流程闭环且不破坏原有能力 |
| Phase 5 | 浏览器视觉与交互验收 | 构建结果、验收截图、问题清单与修复记录 | 1–2 天 | 核心路径、边界状态、布局质量通过 |
| Phase 6 | 评审反馈与交付收口 | 反馈决策表、同步后的文档/原型、交付说明 | 0.5–1 天 | 已接受反馈全部同步，无未声明阻塞项 |

\* 预估投入基于“本地前端原型、Mock 数据、无真实接口”的假设；Phase 1 结束后重新估算。

## 5. 分阶段执行计划

### Phase 0：仓库接入与分支基线

**目标：** 在不覆盖现有工作的前提下，将本地工作区接入远端原型，并从正确基线建立退款风控功能分支。

| files | action | verify | done |
| --- | --- | --- | --- |
| `.git/config`、远端代码（具体文件待拉取） | 确认远端 URL；识别默认分支；检查本地状态；拉取代码；经用户确认后创建 `codex/refund-risk-control` 或用户指定分支 | `git remote -v`、`git status`、`git branch -vv`、最近提交记录 | 本地代码与指定远端基线一致，功能分支可安全开发 |

执行项：

- [ ] 确认是否将 `https://github.com/erduo85-source/RCP.git` 配置为 `origin`
- [ ] 确认远端默认基线分支及目标提交
- [ ] 拉取后检查未跟踪文件、忽略规则与仓库说明
- [ ] 获得明确授权后新建功能分支
- [ ] 记录基线提交 SHA，避免后续版本漂移

门禁：远端接入、基线分支和功能分支均已确认；否则不进入代码修改。

### Phase 1：原型现状盘点与基线验证

**目标：** 读懂原型的技术结构、现有业务边界和视觉规范，避免脱离原项目自行发挥。

| files | action | verify | done |
| --- | --- | --- | --- |
| `README`、包管理文件、路由、页面、组件、样式、Mock、已有产品文档 | 盘点技术栈、启动方式、页面层级、关键流程、公共组件和设计规范 | 安装状态检查、构建/类型检查、浏览器打开主要页面、保存基线截图 | 输出可信的现状清单，明确可复用项、约束和缺口 |

执行项：

- [ ] 读取项目说明、目录结构、依赖与脚本
- [ ] 建立“路由—页面—组件—数据—状态”映射
- [ ] 识别现有退款相关入口、订单/售后对象及已有风险信息
- [ ] 盘点 Ant Design、设计 Token、本地封装组件和图标资源
- [ ] 本地运行原型并走查主流程；记录启动或构建异常
- [ ] 采集主要页面基线截图，标记不得破坏的能力

阶段交付：

- `docs/refund-risk/current-state.md`（建议）
- 页面与组件复用清单
- 基线运行/构建记录与截图目录

### Phase 2：退款风控需求收敛

**目标：** 将“退款风控”从主题收敛为明确的用户任务、业务对象、演示范围和成功标准。

| files | action | verify | done |
| --- | --- | --- | --- |
| 现有需求材料、Phase 1 盘点结果、新建需求收敛文档 | 明确用户、场景、对象、目标、输出、成功标准和非目标；区分本期与未来能力 | 需求评审；逐条检查范围、优先级、依赖和验收标准 | 每个本期需求都能映射到页面、流程和可见验收结果 |

优先澄清：

| 维度 | 需要确认的问题 |
| --- | --- |
| 用户与权限视角 | 谁查看风险、谁处理、谁配置；原型是否只演示单一角色 |
| 触发时机 | 提交退款前、退款申请中、审核中、退款后，哪些节点需要风控能力 |
| 风控对象 | 订单、退款单、用户、支付账户、设备、商品或其关系 |
| 决策输出 | 放行、拦截、转人工、补充材料、观察，还是仅风险提示 |
| 证据解释 | 需要展示哪些风险标签、命中规则、关联行为和时间线 |
| 操作闭环 | 查看 → 判断 → 处置 → 记录 → 返回/继续下一单是否都需演示 |
| 范围边界 | 仅做审核台拓展，还是同时包含策略配置、命中记录、效果分析 |

候选模块（仅作为讨论起点，不视为已确认需求）：

- 退款申请风险概览与等级提示
- 命中规则/风险标签及可解释证据
- 关联订单、账号、设备或支付行为
- 审核处置、备注与操作记录
- 空、加载、失败、正常、多个风险命中等状态
- 策略配置、命中记录或效果分析（如本轮确需演示）

阶段交付：

- `docs/refund-risk/requirement-convergence.md`（建议）
- 需求优先级与范围决策表
- 可验证的验收标准

### Phase 3：信息架构、页面方案与交互闭环

**目标：** 将需求转换为可评审的页面结构和完整交互，不直接跳到编码。

| files | action | verify | done |
| --- | --- | --- | --- |
| 页面方案、流程图、状态矩阵、Mock 数据字典、必要的视觉参考 | 明确入口面、结果/详情面、处置动作、返回路径和重复操作；定义组件与状态 | 方案评审；逐步演练主路径和至少一个边界路径 | 每个需求有承载页面，每个操作有反馈和回退 |

执行项：

- [ ] 确定是在现有页面内扩展、增加抽屉/弹窗，还是新增独立页面
- [ ] 设计主流程：入口 → 查看/查询 → 风险结果 → 处置 → 返回/继续
- [ ] 定义页面层级、模块顺序、字段、按钮、表格与详情区
- [ ] 设计默认、加载、空、错误、成功、禁用、展开/收起状态
- [ ] 设计 Mock 数据：正常、低/中/高风险、多规则命中、证据缺失等场景
- [ ] 校验长文案、长编号、多标签、无数据等后台页面边界

阶段交付：

- `docs/refund-risk/page-and-flow.md`（建议）
- `docs/refund-risk/mock-data-spec.md`（建议）
- 关键页面与状态验收表

### Phase 4：实现拆解与高保真原型开发

**目标：** 按原项目结构完成轻量、可维护、可点击的退款风控原型。

| files | action | verify | done |
| --- | --- | --- | --- |
| 实际页面、组件、样式、路由、Mock、测试文件（Phase 1 后列出准确路径） | 先建立贯通主流程的最小纵切，再补视觉细节和边界状态；复用现有组件与 Token | 增量构建/类型检查；每完成一个纵切即进行浏览器交互检查 | 核心演示流程完整，状态反馈清晰，代码结构便于产品经理继续修改 |

建议实现顺序：

1. 接入入口和最小结果面，打通主路径。
2. 补齐风险摘要、证据详情和处置动作。
3. 补齐 Mock 场景与加载/空/失败等状态。
4. 对齐原型视觉规范、后台密度和中文文案。
5. 清理临时逻辑与无关改动，更新实现文档。

实现约束：

- 不主动增加后端、数据库、认证和真实外部接口。
- 不用绝对定位、裁切、隐藏溢出或多层 CSS 覆盖掩盖布局问题。
- 不做与退款风控无关的重构。
- Mock 数据必须清楚标识，不能伪装成真实依赖已接通。

### Phase 5：浏览器视觉与交互验收

**目标：** 证明原型不仅能构建，而且在真实浏览器中可看、可点、可完整演示。

| files | action | verify | done |
| --- | --- | --- | --- |
| 验收记录、截图、问题清单，以及必要的代码修复 | 运行构建/类型检查；在浏览器走主路径和边界路径；视觉走查并回归 | 记录命令、URL、视口、点击步骤、截图和实际结果 | 无遮挡、异常换行、明显截断和流程断点；已知差异全部说明 |

最低验收矩阵：

| 验收面 | 必查内容 |
| --- | --- |
| 构建质量 | 依赖、启动、构建、类型检查（以项目现有脚本为准） |
| 主流程 | 入口 → 风险结果 → 处置 → 关闭/返回 → 下一次操作 |
| 边界状态 | 至少覆盖正常、加载、空/无风险、错误、高风险多命中 |
| 视觉质量 | 布局、间距、层级、控件、中文换行、表格与详情信息密度 |
| 兼容范围 | 以原项目目标视口为准；若无约定，至少检查常用桌面视口 |
| 回归 | 原有入口和主要流程未被破坏 |

阶段交付：

- `docs/refund-risk/qa-report.md`（建议）
- 验收截图与问题修复记录

### Phase 6：评审反馈闭环与交付

**目标：** 将评审结论同步回需求、方案和代码，形成可继续迭代的干净基线。

| files | action | verify | done |
| --- | --- | --- | --- |
| 反馈记录、需求/方案文档、代码、交付说明 | 对反馈做接受/拒绝/延期/待澄清决策；先改上游文档，再同步原型并回归 | 检查已接受反馈是否落到对应文件；重新执行相关验收 | 文档与原型一致，未决项、剩余风险和查看方式清晰 |

执行项：

- [ ] 建立“来源—模块—反馈—决策—影响—改动—状态”记录
- [ ] 对范围/交互变化先更新需求与页面方案
- [ ] 完成原型修改并回归受影响路径
- [ ] 清理过期“最终版”表述和互相冲突的说明
- [ ] 输出准确的改动文件、验证结果、剩余风险和本地查看方式

## 6. 关键里程碑与决策门

| 里程碑 | 必须回答的问题 | 未通过时的处理 |
| --- | --- | --- |
| M0 基线可用 | 远端、默认分支、目标提交、功能分支是否明确 | 不修改代码 |
| M1 现状可解释 | 原型如何运行、哪些页面/组件可复用、视觉基线是什么 | 先解决运行或材料缺口 |
| M2 范围已锁定 | 本期用户、场景、对象、主流程、非目标是否一致 | 不进入高保真设计和编码 |
| M3 方案可演示 | 入口、结果、处置、返回、重复操作及边界状态是否完整 | 补方案，不用代码掩盖歧义 |
| M4 原型可验收 | 构建、主流程、边界状态、视觉质量和回归是否通过 | 修复并重复浏览器验收 |
| M5 交付一致 | 需求、方案、原型、反馈记录是否同步 | 清理冲突后再交付 |

## 7. 风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 本地为空仓库、远端未配置 | 无法基于原型制定精确文件级计划 | Phase 0 先接入远端；Phase 1 后更新文件清单和工期 |
| “退款风控”范围过宽 | 页面和流程不断扩张 | Phase 2 锁定本期主流程，将策略平台、分析等拆为可选范围 |
| 缺少真实业务规则/数据 | 风险解释和处置流程失真 | 使用明确标注的 Mock 场景，并让字段与决策逻辑接受业务评审 |
| 直接编码导致信息架构返工 | 进度不可控 | M2、M3 通过后再进入完整实现 |
| 只做构建检查、不做视觉验收 | 页面可运行但不可评审 | Phase 5 强制浏览器走查、截图和边界状态检查 |
| 新需求破坏原有流程 | 原型回归 | 记录基线截图，限制无关重构，验收时覆盖原有关键入口 |

## 8. 当前待确认事项

以下事项会影响后续执行方向，进入对应阶段前必须确认：

1. 是否将 `https://github.com/erduo85-source/RCP.git` 配置为本地 `origin` 并拉取。
2. 应从远端哪个分支/提交作为原型基线。
3. 功能分支名称是否使用 `codex/refund-risk-control`。
4. 退款风控本期重点是审核侧、策略侧、数据分析侧，还是其中一个最小闭环。
5. 是否已有 PRD、截图、Figma、评审记录或业务规则可作为接受基准。

## 9. 本计划维护规则

- Phase 0/1 结束后，将“建议路径”替换为仓库中的准确文件路径，并重新估算阶段投入。
- 每个阶段开始前复读本计划；完成后更新状态、实际产物、验证结果和偏差。
- 所有失败与阻塞写入 `progress.md`，关键发现与决策写入 `findings.md`。
- 新评审意见优先于旧的“最终版”描述；接受的反馈必须同步到上游文档和原型。

## 10. 阶段状态

| 阶段 | 状态 | 说明 |
| --- | --- | --- |
| 本轮：生成执行计划 | 已完成 | 已基于本地仓库事实和指定技能形成计划 |
| Phase 0：仓库接入与分支基线 | 待开始 | 等待远端接入与新建分支授权 |
| Phase 1：现状盘点 | 待开始 | 依赖 Phase 0 |
| Phase 2：需求收敛 | 待开始 | 依赖 Phase 1 和业务材料 |
| Phase 3：方案与交互 | 待开始 | 依赖 Phase 2 |
| Phase 4：原型实现 | 待开始 | 依赖 Phase 3 |
| Phase 5：浏览器验收 | 待开始 | 依赖 Phase 4 |
| Phase 6：评审与交付 | 待开始 | 依赖 Phase 5 |

## Errors Encountered

| Error | Attempt | Resolution |
| --- | --- | --- |
| 首次并行读取仓库信息时命令因空仓库返回非零，聚合调用未返回全部结果 | 1 | 改为分别读取技能文件与仓库状态 |
| 系统 `python.exe` 无法执行会话恢复脚本 | 1 | 加载工作区依赖，改用 Codex 内置 Python 后成功执行 |

